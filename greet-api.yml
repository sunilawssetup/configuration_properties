spring:
  application:
     name: GREET-API
  boot:
    admin:
      client:
        url: http://localhost:9090/

server:
  port: 9092

management:
  endpoints:
    web:
      exposure:
        include: '*'